
export default {
    app: {
        title: 'Wooppay',
    },
    server: {
        domain: 'http://api.extended.tpl',
        version: 1,
    }
};

/*
import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
        server: {
            domain: 'http://api.extended.tpl',
            version: 1,
        }
    },
    getters: {
        domain (state) {
            return state.domain
        }
    },
});*/
